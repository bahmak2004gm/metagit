# this an example 
…or create a new repository on the command line

echo "# metagit" >> README.md

### initialize a folder as git repository
git init
## "git add" and "git commit" are git basic command
git add README.md
git commit -m "first commit"

## making "main" as master branch
git branch -M main

##adding remote with name "origin" followed by URL
git remote add origin https://github.com/bahmak2004gm/metagit.git

##merging your repository with the remote.(-u is not necessary, it means default so that later on git push command
##is only nedded
git push -u origin main

########FOR EXISTING LOCAL GIT REPOSITORY

…or push an existing repository from the command line

git remote add origin https://github.com/bahmak2004gm/metagit.git
git branch -M main
git push -u origin main

#########this error happens when editor in global config is not set
[user@WESTEND metagit]$ git commit -a
hint: Waiting for your editor to close the file... error: cannot run vi: No such file or directory
error: unable to start editor 'vi'
Please supply the message using either -m or -F option.

#####
